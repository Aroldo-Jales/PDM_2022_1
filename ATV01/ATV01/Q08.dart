/*8. Utilizando o método forEach de
listas, imprima o dobro dos números
de uma lista de inteiros. */

void main() {
  List list = [1, 2, 3, 5, 6, 7, 8, 9];
  list.forEach((element) => print(element * 2));
}
